# Follower
# The Detection-to-Control Pipeline
YOLOV11 -> VLA -> MAVSDK
coordinates(x,y,w,h) and the label(Language input) -> Follow at a safe distance -> Action -> set_velocity_body command to the flight controller

